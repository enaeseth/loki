/*
 * There isn't really such a thing as the "core" plugin. The core plugin just
 * contains resources (especially language strings) needed by the Loki core.
 */
Loki.Plugin.create("core", {
	name: "Loki Core",
	version: LOKI_VERSION
});
